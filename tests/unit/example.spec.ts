import { shallowMount } from "@vue/test-utils";
import { SimpleNode } from "yfiles";

describe("yFiles", function () {
  it("should not make it difficult", function (done) {
    const node = new SimpleNode();

    done();
  });
});
